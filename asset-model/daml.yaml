# for config file options, refer to
# https://docs.daml.com/tools/assistant.html#project-config-file-daml-yaml

sdk-version: 3.2.0-snapshot.20241106.0
name: asset-model
source: daml
init-script: Main:setup
version: 0.0.1
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script

# These are not used by the daml build tool. It's only used by scripts
# contained in this project. But note that when sdk-version above
# changes, this has to chagne also.
protobuf-url: https://github.com/digital-asset/daml/releases/download/v3.2.0-snapshot.20241106.0
protobuf-filename: protobufs-3.2.0-snapshot.20241031.13398.0.vf95d2607.zip
